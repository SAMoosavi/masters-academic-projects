# نمودارها — لانگنر (۲۰۱۳) «کشتن یک سانتریفیوژ»

<style dir="rtl">

نمودارهای Mermaid بازطراحی‌شده از محتوای فنی
*To Kill a Centrifuge: A Technical Analysis of What Stuxnet's Creators Tried to Achieve*،
رالف لانگنر، گروه لانگنر، نوامبر ۲۰۱۳. ۳۷ صفحه، ۱۳ شکل شماره‌دار، بدون جدول.

> **هشدار انتساب.** این سند *پژوهش بردار حمله و اطلاعات فنی plant* است، **نه** فهرست شاخص‌های نفوذ.
> هیچ نام گواهی، mutex، کلید رجیستری یا دامنهٔ C2 ندارد و **WinStux**، **Stuxnet.RT**،
> تجزیهٔ چهارمؤلفه‌ای، PROFINET/ProfiSAFE، PCS 7 و «۱۲۱ متغیر روتور» را ذکر نمی‌کند.

---

## ۱. سه لایهٔ یک حملهٔ سایبری-فیزیکی (شکل ۱)

```mermaid
flowchart TB
    subgraph IT["لایهٔ فناوری اطلاعات — شبکه‌ها، سیستم‌عامل‌ها، نرم‌افزارهای کاربردی"]
        IT1["نقش: <b>انتشار</b><br/>پخش کردن بدافزار"]
    end
    subgraph ICS["لایهٔ سامانهٔ کنترل صنعتی — کنترلرهای صنعتی،<br/>زیرکنترلرها (مبدل‌های فرکانس، کنترلرهای فشار)"]
        ICS1["نقش: <b>دستکاری</b><br/>دستکاری فرایند، بدون اختلال در آن"]
    end
    subgraph PHY["لایهٔ فیزیکی — شیرها، محرک‌های الکتریکی"]
        PHY1["نقش: <b>آسیب</b><br/>آسیب با بهره‌برداری از آسیب‌پذیری‌های فیزیکی"]
    end
    IT -->|"عفونت به شبکهٔ کنترل می‌رسد"| ICS
    ICS -->|"مقادیر جعلی فرایند؛ اینترلاک‌ها هرگز فعال نمی‌شوند"| PHY

    style IT fill:#e0f0ff,stroke:#06c
    style ICS fill:#fff4d0,stroke:#b80
    style PHY fill:#ffe0e0,stroke:#c00
```

**چرا برای دفاع اهمیت دارد:** کنترل‌های متعارف امنیت اطلاعات فقط **لایهٔ ۱** را هدف می‌گیرند.

---

## ۲. دو محمولهٔ حمله (شکل ۲)

هر دو روتور را نابود می‌کنند، اما از آسیب‌پذیری‌های فیزیکی متفاوت — و **پیچیده‌ترین آن‌ها اول بود**.

```mermaid
flowchart TB
    S["بدافزار استاکس‌نت<br/><b>دو روتین حملهٔ کاملاً متفاوت</b>"]

    S --> A["<b>حملهٔ فشار بیش از حد</b> — روتین «فراموش‌شده»<br/>حدود یک مرتبهٔ بزرگی <b>پیچیده‌تر</b>"]
    S --> B["<b>حملهٔ سرعت روتور</b> — روتین پرتکرار رسانه‌ای<br/>نسبتاً <b>ساده</b>"]

    A --> A1["هدف: <b>سامانهٔ حفاظت آبشار (CPS)</b><br/>روی <b>زیمنس S7-417</b><br/>تا ۶ آبشار = ۹۸۴ سانتریفیوژ"]
    A --> A2["ماشه: «ترکیبی از شرایط بسیار خاص فرایند»<br/>که دائماً پایش می‌شود"]
    A --> A3["جعل داده با <b>بازپخش ۲۱ ثانیه</b> از مقادیر واقعی حسگر"]
    A --> A4["دوره: <b>۲۰۰۷ تا ۲۰۰۹</b> (حملهٔ <b>پیشین</b>)"]

    B --> B1["هدف: <b>سامانهٔ محرک سانتریفیوژ (CDS)</b><br/>روی <b>زیمنس S7-315</b><br/>یک کنترلر = ۱۶۴ محرک"]
    B --> B2["ماشه: دوره‌ای، <b>تقریباً یک بار در ماه</b><br/>اصلاً دادهٔ جعلی ندارد — کد معلق می‌شود"]
    B --> B3["دوره: <b>از ۲۰۰۹</b> (حملهٔ <b>بعدی</b>)"]

    A1 --> R["<b>نتیجهٔ فیزیکی یکسان</b><br/>روتورها زودتر می‌شکنند"]
    A2 --> R
    A3 --> R
    B1 --> R
    B2 --> R
    B3 --> R

    style A fill:#ffe0e0,stroke:#c00
    style B fill:#e0f0ff,stroke:#06c
    style R fill:#fff4d0,stroke:#b80
```

> **افسانه‌ای که این سند تصحیح می‌کند:** بیشتر روایت‌ها ادعا می‌کنند حملهٔ سرعت روتور مقادیر جعلی
> بازپخش می‌کند. **چنین نیست.** بازپخش ۲۱ ثانیه‌ای «تنها در حملهٔ فشار» به کار می‌رود.

---

## ۳. سلسله‌مراتب تأسیسات نطنز

```mermaid
flowchart TB
    FEP["<b>تأسیس غنی‌سازی سوخت (FEP) نطنز</b><br/>غنی‌سازی آبشاری کم‌فناور ایران"]
    FEP --> CU["<b>واحد آبشار = ۱۸ آبشار</b><br/>زیرواحدهای شش‌تایی یک خوراک،<br/>یک محصول و یک ایستگاه دنباله مشترک دارند"]
    CU --> C1["<b>آبشار = ۱۶۴ سانتریفیوژ</b><br/>۴ خط × ۴۳ ستون · ۱۵ مرحلهٔ غنی‌سازی<br/><b>مرحلهٔ ۱۰ = مرحلهٔ خوراک</b><br/>طرح ارائه‌شده توسط A. Q. Khan"]
    C1 --> CF["<b>سانتریفیوژ IR-1</b><br/>موتور اختصاصی در پایه<br/>محرک پایدار تا <b>۱۰۰٬۰۰۰</b> دور بر دقیقه<br/><i>سرعت کار عادی: ۶۳٬۰۰۰</i>"]

    style C1 fill:#fff4d0,stroke:#b80
    style CF fill:#ffe0e0,stroke:#c00
```

**چرا شکل آبشار مهم است:** لوله‌کشی بین‌مرحله‌ای ایران ابتدا بریده و جوش شده بود، پس شکل آبشار بدون
«کار لوله‌کشی اساسی» تغییر نمی‌کرد. اما شکل‌های کوچک‌ترِ بعدی را می‌توان «صرفاً با بستن شیرهای ایزوله،
**همان‌گونه که استاکس‌نت نشان داد**» ایجاد کرد.

---

## ۴. کنترل فشار کم‌فناور ایران و سامانهٔ dump

```mermaid
flowchart LR
    UF6["خوراک UF₆ وارد آبشار می‌شود"] --> ST["مرحلهٔ غنی‌سازی"]
    ST --> PROD["برداشت محصول (غنی‌شده)"]
    ST --> TAILS["برداشت دنباله (تهی‌شده) ← خوراک مرحلهٔ بعد"]
    ST --> PS["<b>مبدل فشار</b> 'PT-'<br/>MKS Baratron<br/>= مقدار <b>اندازه‌گیری‌شده</b>"]
    PS --> PC["<b>کنترلر فشار</b> MKS PR-4000<br/>یکی برای هر مرحله<br/>مقایسهٔ اندازه‌گیری با نقطهٔ تنظیم"]
    PC -->|"اگر بالاتر از نقطهٔ تنظیم،<br/>شیر را باز کند"| EV["<b>شیر اگزوز مرحله</b> 'EP-41xx'<br/>= دستگاه <b>محرک</b>"]
    EV --> DUMP["<b>سامانهٔ dump</b><br/>«هرگز در حالت تولید استفاده نمی‌شود»<br/>پشتیبان برای تریپ آبشار"]
    DUMP -.->|"رفع فشار بیش از حد<br/>به همان مرحله"| ST

    style PS fill:#e0f0ff
    style PC fill:#e0f0ff
    style EV fill:#ffe0e0
    style DUMP fill:#fff4d0,stroke:#b80
```

این «**کنترل پایین‌دستی بنیادی**» است — ابتدایی در برابر آبشارهای پرفناور دیگر کشورها، اما مسیر
رفع فشاری کارآمد به ایران داد؛ و همین دقیقاً چیزی بود که مهاجمون باید خنثی می‌کردند.

---

## ۵. حملهٔ فشار بیش از حد — چگونه روتورها نابود شدند

```mermaid
flowchart TB
    S0["<b>۰. کنترل کامل S7-417</b><br/>«بلافاصله پس از آلودگی، payload این نسخهٔ قدیمی<br/>کنترل را به‌طور کامل در اختیار می‌گیرد.»<br/>منطق مشروع از I/O الکتریکی <b>جدا می‌شود</b><br/>— استقرار یک <b>مرد میانی</b> روی سیگنال‌ها."]
    S0 --> S1["<b>۱. انتظار برای ماشه</b><br/>«ترکیبی از شرایط بسیار خاص فرایند که دائماً<br/>توسط کد مخرب پایش می‌شود»"]
    S1 --> S2["<b>۲. ضبط ۲۱ ثانیه از مقادیر واقعی حسگرها،<br/>سپس بازپخش حلقوی آن‌ها</b><br/>→ صفحه‌های SCADA و منطق آلارم<br/><b>عملکرد کاملاً عادی</b> را می‌بینند"]
    S2 --> S3["<b>۳. ایزوله کردن سانتریفیوژها</b><br/>بستن شیرهای ایزولهٔ مراحل<br/><b>۱، ۲، ۱۴ و ۱۵</b> → انسداد خروج محصول و دنباله.<br/>سپس ایزوله کردن سانتریفیوژهای بیشتر<br/><b>جز در مرحلهٔ خوراک</b>."]
    S3 --> S4["<b>۴. UF₆ وارد می‌شود، چیزی خارج نمی‌شود</b><br/>→ فشار کاری <b>پیوسته</b> بالا می‌رود"]
    S4 --> S5["<b>۵. ناکالیبراسیون حسگرهای فشار</b><br/><i>(فرض صریح نویسنده)</i><br/>بازنویسی خطی‌سازی تا کنترلر «همهٔ خوانش‌های<br/>آنالوگ را کاملاً عادی تفسیر کند».<br/>→ شیرهای اگزوز <b>هرگز باز نمی‌شوند</b>.<br/><b>همچنین حسگرهای هدر خوراک، برداشت محصول<br/>و دنباله + مبدل‌های شمارهٔ ۱۶ تا ۲۱</b>"]
    S5 --> S6["<b>۶. افزایش تنش روتور</b><br/>«فشار دیوارهٔ روتور تابعی از سرعت و فشار کاری است».<br/>UF6 در حدود <b>۱۰۰ میلی‌بار</b> در دمای اتاق جامد می‌شود."]
    S6 --> S7["<b>۷. توقف بر پایهٔ وضعیت</b><br/>«بر پایهٔ پایش وضعیت سانتریفیوژها،<br/><b>به احتمال بیشتر حسگرهای ارتعاش</b>»"]
    S7 --> S8["<b>قصد: افزایش تنش روتور تا <i>زودتر</i> بشکنند —<br/>«اما نه لزوماً در طول اجرای حمله»</b>"]

    style S2 fill:#e0f0ff
    style S5 fill:#ffe0e0,stroke:#c00
    style S8 fill:#fff4d0,stroke:#b80
```

**چرا این حمله دشوار است:** انجماد UF6 «صدها سانتریفیوژ به ازای هر کنترلر آلوده» را نابود می‌کرد،
اما «پرده را کنار می‌زد، زیرا علتش در تحلیل پس از مرگ نسبتاً آسان کشف می‌شد.»

---

## ۶. حملهٔ سرعت روتور — ساده و پرسروصدا

```mermaid
flowchart TB
    R0["<b>۰. تزریق در بالای main executive کنترلر S7-315</b><br/>(شکل ۱۰ — در نرم‌افزار مهندسی قابل مشاهده است)"]
    R0 --> R1["<b>۱. معلق کردن کل کد کنترل مشروع</b><br/>دستور شرطی <b>BLOCK END (BEB)</b> به محیط اجرا<br/>می‌گوید به بالای main executive بازگردد؛<br/>چون این حلقه بی‌نهایت است، حمله تکرار و<br/>همهٔ کدهای بعدی معلق می‌شوند."]
    R1 --> R2["<b>۲. اصلاً نیازی به دادهٔ جعلی نیست</b><br/>سرعت روتور «به‌سادگی توسط مبدل فرکانس تنظیم<br/>و ثابت نگه داشته می‌شود» و <b>متغیر کنترلی نیست</b>.<br/>SCADA حافظهٔ کهنهٔ کنترلر را می‌خواند و<br/>دقیقاً مقادیر پیش از حمله را نشان می‌دهد —<br/>که چون سرعت ثابت است، طبیعی به نظر می‌رسد."]
    R2 --> R3["<b>۳. توالی الف — سرعت بیش از حد</b><br/>۶۳٬۰۰۰ → <b>۸۴٬۶۰۰</b> دور بر دقیقه<br/>به مدت <b>۱۵ دقیقه</b> (شامل شتاب‌گیری چنددقیقه‌ای)"]
    R3 --> R4["<b>۴. توالی ب — اجرای متوالی بعدی</b><br/>رساندن عملاً تا توقف (<b>۱۲۰</b> دور بر دقیقه)،<br/>سپس شتاب‌گیری مجدد — در مجموع <b>۵۰ دقیقه</b>.<br/>مبدل «مانند رخداد ایزوله/کاهش سرعت، نرم decelerate می‌کند»."]
    R4 --> R5["<b>۵. چرا روتورها می‌شکنند</b><br/>IR-1 طرحی <b>فوق‌بحرانی</b> است: بالای سرعت‌های بحرانی<br/>ارتعاش‌کنندهٔ روتور کار می‌کند.<br/>«هر بار که روتور از این سرعت‌های بحرانی، که<br/><b>هارمونیک</b> نامیده می‌شوند، عبور کند می‌تواند بشکند.»<br/>کاهش و شتاب‌گیری مجدد، هر روتور را دوباره از آن‌ها عبور می‌دهد."]
    R5 --> R6["<b>۶. تناوب: تقریباً یک بار در ماه</b>"]
    R6 --> R7["<b>۷. پرسروصدا — و بدون تلاش برای پنهان‌سازی</b><br/>«هیچ تلاشی برای غیرفعال کردن سامانهٔ حفاظت آبشار<br/>در طول حملهٔ سرعت روتور انجام نشده بود» — که<br/>«تنها به قطعه‌ای کوچک از کد حملهٔ فشار نیاز داشت».<br/>مسدود کردن کد کنترل تا یک ساعت برای مهندس کنترل<br/>باتجربه قابل‌تشخیص است؛ و نوسان ۱۶۴ سانتریفیوژ<br/>بین ۶۳٬۰۰۰ تا ۱۲۰ شنیدنی است."]

    style R2 fill:#e0f0ff
    style R5 fill:#fff4d0,stroke:#b80
    style R7 fill:#ffe0e0,stroke:#c00
```

---

## ۷. معماری کنترل و سطح حمله

```mermaid
flowchart TB
    subgraph SCADA["SCADA — زیمنس WinCC (فقط پایش)"]
        W["یک نمونهٔ WinCC<br/>مجموعاً <b>شش آبشار</b> را سرویس می‌دهد<br/><b>هر ۵ ثانیه</b> کنترلرها را برای دادهٔ<br/>خارج از بلوک‌های کنترل قانونی<br/>(<i>تأییدنشده در نطنز</i>)"]
    end
    subgraph MAIN["کنترل اصلی — S7-417 (به ازای هر واحد آبشار)"]
        M["<b>S7-417 / S7-417H</b><br/>سامانهٔ حفاظت آبشار<br/>کنترل شیرها و حسگرهای فشار<br/>تا <b>۶ آبشار (۹۸۴ سانتریفیوژ)</b><br/><b>حملهٔ فشار اینجا اجرا می‌شود</b>"]
    end
    subgraph DRIVE["کنترل محرک — S7-315 (به ازای هر آبشار)"]
        D["<b>S7-315</b><br/>سامانهٔ محرک سانتریفیوژ<br/><b>۱ کنترلر → ۱۶۴ محرک</b><br/><b>حملهٔ سرعت روتور اینجا اجرا می‌شود</b>"]
    end
    subgraph FIELD["سطح میدانی"]
        VFD["<b>مبدل‌های فرکانس</b><br/>منابع تغذیهٔ برنامه‌پذیر؛<br/>تنظیم سرعت با فرکانس AC.<br/>«مستقیماً توسط کنترلر قابل دسترسی نیست»"]
        BUS["<b>شش قطعهٔ PROFIBUS</b><br/>هر یک به یک پردازندهٔ ارتباطی (CP)<br/>روی باس‌بورد S7-315 ختم می‌شود"]
        PC2["<b>کنترلرهای فشار</b> MKS PR-4000<br/>یکی برای هر مرحله،<br/>کنترل حلقه‌بستهٔ شیر،<br/>از راه دروازهٔ PROFIBUS به سریال"]
        IV["<b>شیرهای ایزوله</b> — ۳ شیر سریع‌کناری برای هر سانتریفیوژ"]
        VS["<b>حسگرهای ارتعاش</b> — CPS را برای ایزوله و<br/>توقف دستی واحد معیوب هدایت می‌کنند"]
        M --> PC2
        M --> IV
        M --> VS
    end
    D --> BUS
    BUS --> VFD
    VFD --> MOT["۱۶۴ موتور سانتریفیوژ"]

    W --- M
    W --- D

    style M fill:#ffe0e0,stroke:#c00
    style D fill:#e0f0ff,stroke:#06c
    style W fill:#fff4d0,stroke:#b80
```

**ردّ جنابی (شکل ۱۱):** WinCC آلوده هر **پنج ثانیه** دادهٔ خارج از بلوک‌های قانونی را جست‌وجو می‌کند.

---

## ۸. نفوذ غیرمستقیم از راه اهداف نرم

```mermaid
flowchart LR
    A["<b>مهاجم</b>"] --> B["<b>پیمانکار</b><br/>هدف نرم با دسترسی<br/><i>فیزیکی</i> مشروع"]
    B --> C["<b>رایانه‌های همراه و خودکار USB</b><br/>«وضعیت امنیت سایبری پیمانکاران… قطعاً<br/>هم‌تراز تأسیس نطنز نبود»"]
    C --> D["<b>ورود به محل بدون چالش</b><br/>اتصال به «بحرانی‌ترین سامانه‌های FEP،<br/>بدون آنکه هیچ نگهبانی مانع شود»"]
    D --> E["<b>بیمار صفر</b><br/>لپ‌تاپ مهندسی نطنز"]
    E --> F["<b>بیمار یک</b><br/>«بگویید یک کارخانهٔ سیمان» —<br/>پایین‌دست که شبکه‌اش آلوده شده"]
    F --> G["<b>پیمانکاران دیگر</b> با لپ‌تاپ آلوده"]
    G --> H["<b>دسترسی از راه دور VPN</b><br/>«ویروس در حال سفر قاره‌به‌قاره… نه به دلیل اینترنت،<br/>بلکه به این دلیل که اتصالات شبکهٔ مورد اعتماد<br/>امروز از راه اینترنت تونل می‌شوند»"]

    style C fill:#fff4d0,stroke:#b80
    style H fill:#ffe0e0,stroke:#c00
```

**انتشار عمداً محدود:** «انتشار تنها میان رایانه‌هایی ممکن است که به یک شبکهٔ منطقی متصل‌اند یا از راه
خودکار USB فایل رد و بدل می‌کنند.»

**بدون کلید توقف:** جست‌وجوی سادهٔ نام فایل **`s7otbxsx.dll`** کافی بود.

---

## ۹. گاه‌شمار

```mermaid
flowchart LR
    A1["<b>۲۰۰۷</b><br/>فردی ناشناس نمونه‌ای را به<br/><b>Virustotal</b> می‌فرستد.<br/>هیچ شرکت آنتی‌ویروسی نمی‌فهمد."] --> A2["<b>۲۷ ژوئن ۲۰۰۷</b><br/>کارزار در نطنز آغاز می‌شود.<br/>محمولهٔ فشار در هر کنترلر آلوده<br/><b>غیرفعال</b> است."]
    A2 --> B1["<b>۲۰۰۷ تا ۲۰۰۹</b><br/>حملهٔ فشار اجرا می‌شود.<br/>روتین سرعت روتور غیرفعال می‌ماند."]
    B1 --> B2["<b>۲۰۰۹</b><br/>«مهاجمون تصمیم گرفتند<br/>چیز متفاوتی را امتحان کنند»"]
    B2 --> C1["<b>۲۰۱۰</b><br/>شرکت بلاروسی <b>VirusBlokAda</b><br/>عملاً با بدافزار برخورد می‌کند.<br/>نسخهٔ سرعت روتور «دست‌کم یک سال<br/>در محیط وحش» گشت."]
    C1 --> C2["<b>اوت ۲۰۱۰</b><br/>ایران سرورهای C2 را مسدود می‌کند<br/>→ استاکس‌نت «کاملاً خودسرانه<br/>اجرا می‌کرد»"]
    C2 --> D1["<b>۲۰۱۱ تا ۲۰۱۲</b><br/>افشای مرزهای مراحل غنی‌سازی؛<br/>سپس گزارش دیوید سنگر<br/>در نیویورک تایمز"]

    style A2 fill:#ffe0e0,stroke:#c00
    style C1 fill:#fff4d0,stroke:#b80
```

نام عملیاتی ادعایی: **«عملیات المپیک گیمز»**.

---

## ۱۰. چرا دفاع‌های متعارف کار نمی‌کنند

```mermaid
flowchart TB
    subgraph C1["جداسازی هوایی"]
        A1["بی‌اعتبار: «جداسازی هوایی اهداف باارزش را می‌توان پرید،<br/>یعنی با آلوده کردن <b>رایانه‌های همراه پیمانکاران</b>»"]
    end
    subgraph C2["آنتی‌ویروس"]
        A2["امضایی برای بدافزار اختصاصی وجود ندارد.<br/>نمونهٔ ۲۰۰۷ <b>شش سال</b> طول کشید تا نام‌گذاری شود؛<br/>نسخهٔ ۲۰۱۰ دست‌کم <b>یک سال</b>."]
    end
    subgraph C3["وصله‌ها"]
        A3["فروشندهٔ ICS «<b>در ابتدا وجود هر آسیب‌پذیری را انکار کرد</b>»؛<br/><b>CVE-2012-3015</b> دو سال بعد ثبت شد.<br/>همچنان وصله‌نشده: جعل مقادیر حسگر از راه تصویر فرایند؛<br/>ربایت DLL درایور."]
    end
    subgraph C4["IDS / IPS"]
        A4["«ایدهٔ درخشان کشف حملات سایبری-فیزیکی پیچیده در ترافیک شبکه<br/><b>کاملاً اعتبارسنجی‌نشده</b> است».<br/>«دفاع در برابر آن‌ها در حد میلی‌ثانیه ممکن نیست؛<br/>مستلزم <b>سال‌ها تغییر سازمانی و معماری</b> است.»"]
    end
    C1 --> Z
    C2 --> Z
    C3 --> Z
    C4 --> Z
    Z["<b>نتیجهٔ لانگنر</b><br/>حمله «بسیار اختصاصی بود، اما تاکتیک‌ها و فناوری حمله<br/>اختصاصی نیستند؛ عمومی‌اند و می‌توان علیه<br/>اهداف دیگر نیز استفاده شوند.»<br/>«تقریباً <b>۳۰ کشور</b> برنامه‌های سایبری تهاجمی دارند،<br/>از جمله کرهٔ شمالی، ایران، سوریه و تونس.»"]

    style Z fill:#ffe0e0,stroke:#c00
```

---

## ۱۱. درس‌هایی که استاکس‌نت به مهاجمون آینده می‌دهد

```mermaid
mindmap
  root((درس‌های استاکس‌نت))
    تصاحب کنترلر
      تزریق کد روی کنترلرهای بلادرنگ
      ربایت یک DLL درایور
      گفت‌وگوی مستقیم با کنترلرهای شبکه‌ای
      در اختیار گرفتن کنترل از برنامهٔ مشروع در حال اجرا
      غیرفعال کردن کد مشروع با یک دستور پرش
      ربایت توابع کتابخانهٔ کنترلر
    فریب اپراتور
      جعل دادهٔ حسگر با نوشتن در تصویر فرایند ورودی
      غیرفعال کردن پایش زمان چرخهٔ کنترلر
    رسیدن به زیرکنترلرها
      تسخیر زیرکنترلرها با پیکربندی مجدد
      واسط مستقیم با تجهیزات میدانی از راه PROFIBUS
      کور کردن حسگرها با ناکالیبراسیون
    بدون نیاز به zero-day
      «در فضای ICS بدترین آسیب‌پذیری‌ها باگ نیستند، قابلیت‌اند»
      باگ‌ها با یک وصلهٔ سریع رفع می‌شوند، قابلیت‌ها نه
    یافتن هدف
      مطالعهٔ HAZOP و تحلیل‌های ایمنی
      جست‌وجوی سامانه‌های حفاظتی و ایمنی
    ورود به سیستم
      نفوذ غیرمستقیم از راه اهداف نرم و پیمانکاران
    تکرار نتیجه
      یک نسخهٔ کامل نیازمند تخت آزمایش UF6 است = دولت‌ـ‌ملت
      اما کپی‌کتی‌های واقعی فقط زیرساخت استاندارد فراوان می‌خواهند
      نیروگاه برق، پست‌های برق، کارخانه‌های شیمیایی
      حملهٔ هم‌زمان و کم‌جاذبه به اهداف متعدد بر یک ضربهٔ گران
```

</div>
